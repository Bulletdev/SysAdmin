<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>SysAdmin - Gestão de Usuários</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Tailwind CSS via asset pipeline (skip in test to avoid SassC/Tailwind import errors) -->
    <% unless Rails.env.test? %>
      <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <% end %>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Lexend:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <% unless Rails.env.test? %>
      <script src="https://cdn.tailwindcss.com"></script>
    <% end %>
    <style type="text/tailwindcss">
    @layer utilities {
      .gradient-text {
        background: linear-gradient(90deg, #0ea5e9 0%, #f97316 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
      
      .btn-primary {
        background: linear-gradient(90deg, #0ea5e9 0%, #0284c7 100%);
        transition: all 0.3s ease;
      }
      
      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(14, 165, 233, 0.3);
      }
      
      .btn-accent {
        background: linear-gradient(90deg, #f97316 0%, #ea580c 100%);
        transition: all 0.3s ease;
      }
      
      .btn-accent:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(249, 115, 22, 0.3);
      }
      
      .card-hover {
        transition: all 0.3s ease;
      }
      
      .card-hover:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      }
      
      .nav-link {
        position: relative;
        transition: color 0.3s ease;
      }
      
      .nav-link::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #0ea5e9 0%, #f97316 100%);
        transition: width 0.3s ease;
      }
      
      .nav-link:hover::after {
        width: 100%;
      }
      
      .bg-gradient-primary {
        background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%);
      }
      
      .bg-gradient-accent {
        background: linear-gradient(135deg, #f97316 0%, #ea580c 100%);
      }
      
      .backdrop-blur {
        backdrop-filter: blur(10px);
      }
      
      /* Mobile menu animations */
      .mobile-menu {
        transform: translateX(100%);
        transition: transform 0.3s ease;
      }
      
      .mobile-menu.open {
        transform: translateX(0);
      }
      
      /* Loading animation */
      .loading-spinner {
        border: 3px solid #f3f3f3;
        border-top: 3px solid #0ea5e9;
        border-radius: 50%;
        width: 40px;
        height: 40px;
        animation: spin 1s linear infinite;
      }
      
      @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }
      
      /* Responsive utilities */
      @media (max-width: 640px) {
        .mobile-stack {
          flex-direction: column;
        }
        
        .mobile-full-width {
          width: 100%;
        }
        
        .mobile-text-center {
          text-align: center;
        }
      }
    }
  </style>
    <!-- Chart.js UMD for global Chart variable -->
    <% unless Rails.env.test? %>
      <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.js"></script>
      <%= javascript_importmap_tags %>
    <% end %>
  </head>

  <body class="bg-gray-50 font-sans">
    <!-- Navigation -->
    <nav class="bg-white/90 backdrop-blur-md border-b border-gray-200 sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <a href="/" class="flex-shrink-0 flex items-center group">
              <%= image_tag "/SysAdminLogo.png", alt: "SysAdmin", class: "h-10 w-auto rounded-lg group-hover:scale-105 transition-transform duration-200" %>
            </a>
            
            <!-- Desktop Navigation -->
            <div class="hidden md:ml-10 md:flex md:items-center md:space-x-8">
              <% if user_signed_in? %>
                <a href="<%= root_path %>" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200 border-b-2 border-transparent hover:border-blue-600">Página Inicial</a>
                <% if current_user.admin? %>
                  <a href="<%= admin_dashboard_path %>" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200 border-b-2 border-transparent hover:border-blue-600">Dashboard</a>
                <% end %>
                <a href="<%= solutions_path %>" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200 border-b-2 border-transparent hover:border-blue-600">Soluções</a>
                <a href="<%= user_profile_path %>" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200 border-b-2 border-transparent hover:border-blue-600">Perfil</a>
              <% else %>
                <a href="<%= solutions_path %>" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200">Soluções</a>
              <% end %>
            </div>
          </div>
          
          <!-- User Menu -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <div class="flex items-center space-x-4">
                <div class="hidden sm:flex items-center space-x-3 group">
                  <% if current_user.avatar_image.present? %>
                    <%= image_tag current_user.avatar_image, alt: "Avatar", class: "w-8 h-8 rounded-full object-cover" %>
                  <% else %>
                    <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-500 rounded-full flex items-center justify-center">
                      <span class="text-white text-sm font-semibold"><%= current_user.full_name&.first&.upcase || current_user.email.first.upcase %></span>
                    </div>
                  <% end %>
                  <div class="text-sm">
                    <p class="text-gray-900 font-medium">Bem-vindo de volta, <%= current_user.full_name || current_user.email.split('@').first %>!</p>
                    <% if current_user.admin? %>
                      <p class="text-gray-500 text-xs">Acesse o Dashboard pelo menu</p>
                    <% else %>
                      <p class="text-gray-500 text-xs">Acesse seu Perfil pelo menu</p>
                    <% end %>
                  </div>
                </div>
                <%= button_to destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-red-600 px-3 py-2 rounded-lg text-sm font-medium transition-colors duration-200 hover:bg-red-50" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                  </svg>
                <% end %>
              </div>
            <% else %>
              <div class="hidden md:flex items-center space-x-3">
                <a href="<%= new_user_session_path %>" class="text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors duration-200">Entrar</a>
                <a href="<%= new_user_registration_path %>" class="bg-gradient-to-r from-blue-600 to-purple-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:shadow-lg transition-all duration-200 hover:scale-105">Cadastrar</a>
              </div>
            <% end %>
            
            <!-- Mobile menu button -->
            <button id="mobile-menu-button" class="md:hidden p-2 rounded-md text-gray-700 hover:text-primary-600 hover:bg-primary-50">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              </svg>
            </button>
          </div>
        </div>
        
        <!-- Mobile Navigation -->
        <div id="mobile-menu" class="mobile-menu fixed top-0 right-0 h-full w-64 bg-white shadow-xl z-50 md:hidden">
          <div class="p-4">
            <button id="close-mobile-menu" class="float-right p-2 rounded-md text-gray-700 hover:text-primary-600">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
            </button>
          </div>
          <div class="px-4 py-6 space-y-4">
            <% if user_signed_in? %>
              <a href="<%= root_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Página Inicial</a>
              <% if current_user.admin? %>
                <a href="<%= admin_dashboard_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Dashboard</a>
              <% end %>
              <a href="<%= solutions_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Soluções</a>
              <a href="<%= user_profile_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Perfil</a>
              <%= button_to destroy_user_session_path, method: :delete, class: "block w-full text-left text-primary-600 hover:text-primary-700 px-3 py-2 text-base font-medium" do %>
                Sair
              <% end %>
            <% else %>
              <a href="<%= solutions_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Soluções</a>
              <a href="<%= new_user_session_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Entrar</a>
              <a href="<%= new_user_registration_path %>" class="block text-gray-700 hover:text-primary-600 px-3 py-2 text-base font-medium">Cadastrar</a>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-50 border-l-4 border-green-400 p-4 m-4 rounded-r-lg animate-fade-in">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-green-700"><%= notice %></p>
          </div>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-50 border-l-4 border-red-400 p-4 m-4 rounded-r-lg animate-fade-in">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-red-700"><%= alert %></p>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="min-h-screen">
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-gradient-to-br from-gray-900 to-gray-800 text-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- Company Info -->
          <div class="col-span-1 md:col-span-2">
            <div class="flex items-center mb-6">
              <%= image_tag "/SysAdminLogo.png", alt: "SysAdmin", class: "w-12 h-12 rounded-xl" %>
              <span class="ml-4 text-2xl font-bold bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent font-lexend"></span>
            </div>
            <p class="text-gray-300 text-lg mb-6 max-w-md">Plataforma empresarial de gestão de usuários com autenticação segura, interface moderna e recursos administrativos avançados.</p>
            <div class="flex space-x-4">
              <a href="#" class="text-gray-400 hover:text-blue-400 transition-colors">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"/>
                </svg>
              </a>
              <a href="#" class="text-gray-400 hover:text-blue-400 transition-colors">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                </svg>
              </a>
              <a href="#" class="text-gray-400 hover:text-blue-400 transition-colors">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12.017 0C5.396 0 .029 5.367.029 11.987c0 5.079 3.158 9.417 7.618 11.174-.105-.949-.199-2.403.041-3.439.219-.937 1.406-5.957 1.406-5.957s-.359-.72-.359-1.781c0-1.663.967-2.911 2.168-2.911 1.024 0 1.518.769 1.518 1.688 0 1.029-.653 2.567-.992 3.992-.285 1.193.6 2.165 1.775 2.165 2.128 0 3.768-2.245 3.768-5.487 0-2.861-2.063-4.869-5.008-4.869-3.41 0-5.409 2.562-5.409 5.199 0 1.033.394 2.143.889 2.741.099.12.112.225.085.345-.09.375-.293 1.199-.334 1.363-.053.225-.172.271-.402.165-1.495-.69-2.433-2.878-2.433-4.646 0-3.776 2.748-7.252 7.92-7.252 4.158 0 7.392 2.967 7.392 6.923 0 4.135-2.607 7.462-6.233 7.462-1.214 0-2.357-.629-2.75-1.378l-.748 2.853c-.271 1.043-1.002 2.35-1.492 3.146C9.57 23.812 10.763 24.009 12.017 24.009c6.624 0 11.99-5.367 11.99-11.988C24.007 5.367 18.641.001.012.001z.005z"/>
                </svg>
              </a>
            </div>
          </div>
          
          <!-- Quick Links -->
          <div>
            <h3 class="text-lg font-semibold mb-6 text-gray-100">Links Rápidos</h3>
            <ul class="space-y-3 text-gray-300">
              <li><a href="/" class="hover:text-blue-400 transition-colors flex items-center"><span class="mr-2">→</span>Início</a></li>
              <% if user_signed_in? && current_user.admin? %>
                <li><a href="<%= admin_users_path %>" class="hover:text-blue-400 transition-colors flex items-center"><span class="mr-2">→</span>Administração</a></li>
              <% end %>
              <li><a href="<%= user_profile_path %>" class="hover:text-blue-400 transition-colors flex items-center"><span class="mr-2">→</span>Perfil</a></li>
            </ul>
          </div>
          
          <!-- Contact Info -->
          <div>
            <h3 class="text-lg font-semibold mb-6 text-gray-100">Contato</h3>
            <div class="space-y-3 text-gray-300">
              <p class="flex items-center"><span class="mr-3">📧</span>suporte@sysadmin.com</p>
              <p class="flex items-center"><span class="mr-3">📞</span>+55 (11) 99999-9999</p>
              <p class="flex items-center"><span class="mr-3">🌐</span>São Paulo, Brasil</p>
            </div>
            <div class="mt-6">
              <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                <span class="w-2 h-2 bg-green-400 rounded-full mr-2"></span>
                Sistema Online
              </span>
            </div>
          </div>
        </div>
        
        <!-- Bottom Bar -->
        <div class="border-t border-gray-700 mt-12 pt-8">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <div class="text-gray-400 mb-4 md:mb-0">
              <p>&copy; <%= Date.current.year %> SysAdmin Enterprise. Todos os direitos reservados.</p>
            </div>
            <div class="flex space-x-6 text-sm text-gray-400">
              <a href="#" class="hover:text-blue-400 transition-colors">Termos de Serviço</a>
              <a href="#" class="hover:text-blue-400 transition-colors">Política de Privacidade</a>
              <a href="#" class="hover:text-blue-400 transition-colors">Segurança</a>
            </div>
          </div>
        </div>
      </div>
    </footer>
    
    <!-- Mobile Menu Script -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const closeMobileMenu = document.getElementById('close-mobile-menu');
        const mobileMenu = document.getElementById('mobile-menu');
        
        mobileMenuButton.addEventListener('click', function() {
          mobileMenu.classList.add('open');
        });
        
        closeMobileMenu.addEventListener('click', function() {
          mobileMenu.classList.remove('open');
        });
        
        // Close menu when clicking outside
        document.addEventListener('click', function(event) {
          if (!mobileMenu.contains(event.target) && !mobileMenuButton.contains(event.target)) {
            mobileMenu.classList.remove('open');
          }
        });
      });
    </script>
  </body>
</html>
